.row
  .col-md-12
    .text-center
      | All Users

table.table.table-bordered.table-striped.tablesorter-blue#all-users-table
  thead
    tr
      th First name
      th Last name
      th Skype ID
      th Profile Link
  tbody
    - @users.each do |user|
      tr
        td = user.first_name
        td = user.last_name
        td = user.skype_id
        td = link_to "View", user_path(user)